version: "2"
sql:
  - engine: postgresql
    schema: sql/migrations/
    queries: sql/queries/
    gen:
      go:
        package: tododb
        out: internal/tododb
        sql_package: pgx/v5
        emit_json_tags: true
        initialisms:
          - "id"
          - "url"
